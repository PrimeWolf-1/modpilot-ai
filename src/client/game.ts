// ModPilot AI — Dashboard client entry point
// Full implementation in Phase 3 (items 9-12)
// This file is the compiled entry for game.html (the expanded dashboard view)

import { ApiEndpoint } from "../shared/api.ts";
import type { GetQueueResponse, GetStatsResponse } from "../shared/types.ts";

// Phase 3 will build the full dashboard UI here.
// For now, expose the API endpoint constants for type-checking.
export { ApiEndpoint };
export type { GetQueueResponse, GetStatsResponse };
